<!-- Navbar -->
<nav class="navbar navbar-expand-lg nav-shadow flex-column p-0 bg-transparent">
  <div class="bg-main py-1 text-white w-100">
    <div class="container">
      <small>犬・猫の病気・けが しつけ・介護・食事などの悩みをみんなで解決しよう (仮テキスト)</small>
    </div>
  </div>
  <div class="container py-2 bg-white">
    <div class="d-flex justify-content-start align-items-center gap-3">
      <a class="navbar-brand" href="<%= my_page_path %>">
        <img src="/images/logo.png" alt="Logo" style="width: 230px">
      </a>
      <div style="width: 200px">
        <%= form_tag(pets_path, method: :get, class: "input-group border border-dark rounded") do %>
          <%= text_field_tag :search, params[:search], placeholder: "ペットを探す", class: "form-control" %>
          <div class="input-group-btn fw-bold">
            <button class="btn bg-transparent fw-bold" type="submit">
              <i class="fa fa-search"></i>
            </button>
          </div>
        <% end %>
      </div>
    </div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
      <ul class="navbar-nav me-4 mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= communities_path %>">コミュニティ</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= all_events_path%>">イベント</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= all_posts_path %>">トピック</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= questions_path %>">Q&A</a>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link active ml-2" aria-current="page" href="<%= notifications_path %>">
              <div class="position-relative">
                <i class="far fa-bell fa-lg"></i>
                <% if current_user.notifications.unread.count > 0 %>
                <span class="position-absolute rounded-circle d-flex justify-content-center align-items-center top-50 start-50 text-white fw-bold bg-main" style="width: 20px; height: 20px; font-size: smaller !important"><%= current_user.notifications.unread.count %></span>
                <% end %>
              </div>
            </a>
          </li>
          
          <li class="nav-item">
            <a class="nav-link active ml-2" aria-current="page" href="<%= notifications_path %>">
              <div class="position-relative">
                <i class="far fa-envelope fa-lg"></i>
                <% if current_user.messagee.unread.count > 0 %>
                <span class="position-absolute rounded-circle d-flex justify-content-center align-items-center top-50 start-50 text-white fw-bold bg-main" style="width: 20px; height: 20px; font-size: smaller !important"><%= current_user.messagee.unread.count %></span>
                <% end %>
              </div>
            </a>
          </li>
          <li class="nav-item">
            <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "nav-link active btn text-white bg-light-main ml-4"  %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "会員登録", new_user_registration_path, class: "nav-link active btn text-white bg-light-main ml-4"  %>
          </li>
          <li class="nav-item">
            <%= link_to "ログイン", new_user_session_path, class: "nav-link active btn text-white bg-light-main ml-4"  %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
