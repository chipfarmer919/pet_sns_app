
<div class="container" style="margin-top: 3rem">
  <h4 class="d-flex align-items-center justify-content-center gap-2 fw-bold mb-4"><i class="fa fa-paw text-light-main"></i> 本会員登録</h4>
  
  <div class="mx-auto mt-4" style="max-width: 400px; ">
    <%= form_for @user, url: save_additional_info_path, method: :post, html: { id: 'additional-info-form' } do |f| %>
    
      <div class="field mb-3">
        <%= f.label :会員登録が完了しました。 %><br>
        <%= f.label :サービス利用前にあなたのペットの情報を登録してください。 %><br>
      </div>
      
      <div class="field mb-3">
        <%= f.label :"お名前", class: "form-label fw-bold" %><br>
        <%= f.text_field :username, placeholder: "ニックネーム", required: true, class: "form-control" %>
      </div>      
      


      <div class="field mb-3">
        <%= f.label :"所在地", class: "form-label fw-bold" %>
      </div> 
      <div class="d-flex gap-2 field mb-3">
        <div class"flex-col flex-x-start">
          <%= f.label :都道府県, class: "form-label fw-bold" %><br>
          <%= f.select :prefecture, options_for_select(prefectures_with_cities.keys), { prompt: "都道府県を選択" }, id: 'prefecture-select', class: 'form-select' %>
        </div>
        <div class"flex-col flex-x-start">
          <%= f.label :市町村, class: "form-label fw-bold" %><br>
          <%= f.select :city, options_for_select([]), { prompt: "市町村を選択" }, id: 'city-select', class: 'form-select' %><br>
        </div>
      </div>
      <div class="flex-col flex-x-center my-20px">
        <%= f.submit "入力内容を確認する", class:"btn btn-lg fw-bold text-white w-100 bg-main" %>
      </div>
    <% end %>
  </div>
</div>